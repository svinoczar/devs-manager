# JWT Secret (generate with: openssl rand -hex 128)
SECRET_KEY=super-secret-jwt-key-change-this-in-production

# Encryption key for GitHub tokens (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=encryption-key-for-github-tokens
GITHUB_TOKEN=
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=